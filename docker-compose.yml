services:
  bot:
    build: .
    container_name: discord-stock-bot
    env_file: .env
    restart: unless-stopped
    volumes:
      - ./data:/app/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
